if [[ -n $SSH_CONNECTION ]]; then
  hostStyle="%F{yellow}"
else
  hostStyle="%F{cyan}"
fi

PROMPT=$'\n'
PROMPT+=%B
PROMPT+="%(!.%F{red}.%F{blue})%n%f"
PROMPT+="%F{white} at %f"
PROMPT+="$hostStyle%m%f"
PROMPT+="%F{white} in %f"
PROMPT+="%F{green}%~%f"
#PROMPT+="%F{105}%~%f"
PROMPT+=$'\n'
PROMPT+="%F{white}%#%f "
PROMPT+=%b
#PROMPT+="%(?..%F{red}● )%f"    # return status
